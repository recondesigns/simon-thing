import { createTheme } from "@mui/material/styles";

// Placeholder theme. Palette/typography will be replaced once the real
// designs land (see checklist: "Set up color palette/theme in MUI").
const theme = createTheme({
  cssVariables: true,
});

export default theme;
